def add(x: int, y: int) -> int:
    """Take two integers and returns the sum of them.

    Args:
        x (int): first number
        y (int): second number

    Returns:
        int: the sum of x + y
    """
    return x + y
